#
# Debian configure command for tcl.gd
#
# Requires tcl-dev and libgd2-noxpm-dev or libgd2-xpm-dev
#
# apt-get install tcl8.4-dev
# apt-get install libgd2-noxpm-dev
#

autoreconf
./configure --with-gd-lib=/usr/lib --enable-shared --enable-symbols

